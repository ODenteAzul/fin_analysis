CREATE TABLE IF NOT EXISTS {camda}.{tabela} (
                data DATE PRIMARY KEY,
                selic NUMERIC,
                ipca NUMERIC,
                pib NUMERIC,
                juros_usa NUMERIC,
                dolar NUMERIC,
                ibovespa NUMERIC
            );